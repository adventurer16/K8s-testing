apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-staging
  labels:
    app: mongodb
    env: staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mongodb
      env: staging
  template:
    metadata:
      labels:
        app: mongodb
        env: staging
    spec:
      containers:
      - name: mongodb
        image: mongo:latest
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: mongodb-data
          mountPath: /data/db
      volumes:
      - name: mongodb-data
        persistentVolumeClaim:
          claimName: mongodb-pvc-staging
