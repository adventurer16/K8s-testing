apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImageRepository
metadata:
  name: frontend-image-repo
  namespace: flux-system
spec:
  image: rabebhamd/frontend-service
  interval: 1m # How frequently to check for new images
